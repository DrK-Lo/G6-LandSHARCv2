#include <R.h> 
#include <Rdefines.h>
#include <Rmath.h> 
#include <stdio.h>
#include <math.h>
#include <stdlib.h>

void Dist(double *u, double *P_freqMAT, int *length)
{
	int i;
	double sigma=0.5
	double u =5
	for (i=0; i<*length; i++) {
		rnorm(u, sigma)
	}	
}